# Отчёты тестирования

После запуска:

```bash
python tests/test_runner.py
```

здесь появляются файлы:

- `latest_report.json` — последний отчёт (удобно для сдачи)
- `test_report_YYYYMMDD_HHMMSS.json` — отчёты с датой

Положите актуальный `latest_report.json` в коммит перед сдачей второй версии.
